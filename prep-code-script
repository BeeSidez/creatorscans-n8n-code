const cleanText = (text) => {
  if (!text) return "";
  return text
    .replace(/[\u{1F600}-\u{1F64F}]/gu, "")
    .replace(/[\u{1F300}-\u{1F5FF}]/gu, "")
    .replace(/[\u{1F680}-\u{1F6FF}]/gu, "")
    .replace(/[\u{1F1E0}-\u{1F1FF}]/gu, "")
    .replace(/[\u{2600}-\u{26FF}]/gu, "")
    .replace(/[\u{2700}-\u{27BF}]/gu, "")
    .replace(/[\u{FE00}-\u{FE0F}]/gu, "")
    .replace(/[\u{200D}]/gu, "")
    .replace(/[\u{20E3}]/gu, "")
    .replace(/[\u{E0020}-\u{E007F}]/gu, "")
    .replace(/[\u{1F900}-\u{1F9FF}]/gu, "")
    .replace(/[\u{1FA00}-\u{1FA6F}]/gu, "")
    .replace(/[\u{1FA70}-\u{1FAFF}]/gu, "")
    .replace(/[\*\(\)\[\]\•\n]/g, "")
    .replace(/[^\x20-\x7E\s]/g, "")
    .replace(/\s+/g, " ")
    .trim();
};

// --- GCS upload (for Vertex AI file input) ---
const gcs = $('GCS Upload').first().json;
const gcsFileName   = gcs.name || '';
const gcsBucket     = gcs.bucket || '';
const gcsFileUri    = `gs://${gcsBucket}/${gcsFileName}`;

// --- Cloudinary upload (for screenshot URLs in storyboard output) ---
const cloudinary        = $('Cloudinary Upload').first().json;
const cloudinaryPublicId = cloudinary.public_id || '';

return [{
  json: {
    gcsFileUri,
    cloudinaryPublicId,
  }
}];
